# R1 commit history

This file records commits intentionally attributed to [R1](R1.md). A commit is
listed only when its scope and verification are known; uncommitted work is not
history.

| Commit | Date | Relationship to R1 |
|---|---|---|
| `14646a9` — Archive router benchmark baselines | 2026-09-04 | Supporting comparison baseline; not R1 implementation by itself |
| `b8160bb` — Add opt-in routing-aware placement policy | 2026-09-04 | Introduces the opt-in policy surface used by the R1 workstream |
| `7c68af4` — Define exact pin-access catalog contracts | 2026-09-04 | Adds exact pin-access catalog contracts used by routability-by-construction work |

Future entries should include the verification command or evidence location in
the commit message or an adjacent note.
